/**
 * lobby.h - Online lobby for MMM
 */

#ifndef MMM_LOBBY_H
#define MMM_LOBBY_H

void lobby_init(void);
void lobby_screen(void);    /* Per-frame entry; returns immediately if not in this state */

#endif
